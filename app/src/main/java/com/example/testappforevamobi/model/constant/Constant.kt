package com.example.testappforevamobi.model.constant

import com.example.testappforevamobi.view.activity.MainActivity

lateinit var MAIN:MainActivity

const val CURRENT_RESULT = "CURRENT_RESULT"
const val BEST_RESULT = "BEST_RESULT"

val listItemsForGame = (1..99).toList()

const val WIN = "WIN"
const val LOSS = "LOSS"